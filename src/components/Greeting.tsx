// src/Greeting.tsx
import React from "react";
import { Typography } from "@mui/material";

const Greeting: React.FC = () => {
  return <Typography variant="h4">Hello, Parkly!</Typography>;
};

export default Greeting;
